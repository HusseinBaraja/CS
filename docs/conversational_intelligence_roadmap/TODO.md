# Conversational Intelligence TODO

## Deferred Options

- Consider extending assistant structured output with richer semantic fields so `AssistantSemanticRecord` can be built directly from model output instead of deterministic projection from existing runtime artifacts.