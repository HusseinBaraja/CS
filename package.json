{
  "$schema": "./package.schema.json",
  "name": "cs",
  "private": true,
  "packageManager": "bun@1.3.9",
  "workspaces": [
    "apps/*",
    "packages/*",
    "convex"
  ],
  "scripts": {
    "dev": "turbo run dev --filter=api --filter=bot --filter=web --filter=worker",
    "web": "bun run dev:web",
    "dev:api": "turbo run dev --filter=api",
    "dev:bot": "turbo run dev --filter=api --filter=bot",
    "dev:web": "turbo run dev --filter=web",
    "dev:worker": "turbo run dev --filter=worker",
    "build": "turbo run build",
    "build:web": "turbo run build --filter=web",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "test": "bun run test:root && turbo run test",
    "test:root": "bun test ./scripts --pass-with-no-tests",
    "check:ai": "bun scripts/ai-provider-check.ts",
    "test:convex": "bun x vitest run --config convex/vitest.config.ts",
    "check": "turbo run check",
    "preview:web": "bun --cwd apps/web run preview",
    "generate": "bunx convex codegen",
    "seed": "bun run --cwd apps/cli seed",
    "backup": "bun run --cwd apps/cli backup",
    "ci": "turbo run ci"
  },
  "dependencies": {
    "convex": "1.32.0"
  },
  "devDependencies": {
    "@edge-runtime/vm": "^5.0.0",
    "@types/bun": "1.3.9",
    "convex-test": "^0.0.41",
    "turbo": "^2.9.3",
    "typescript": "5.9.3",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
